package javalanguageplayground.version12;

import org.junit.jupiter.api.Test;

public class TeeingCollectorTest {

	@Test
	public void test() {

//		Stream.of(1, 2, 3, 4, 5).collect(Collectors.teeing(
//				Collectors.filtering(i -> i % 2 == 0, Collectors.toList()),
//				Collectors.flatMapping(j -> Math.sqrt(j), Collectors.toList()), 
//				(i, j) -> "The square root of " + i + " is: " + j));

	}

}
